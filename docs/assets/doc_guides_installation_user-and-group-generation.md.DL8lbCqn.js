import{_ as t,c as a,a2 as i,o as n}from"./chunks/framework.BQmytedh.js";const o="/assets/First_login.BPvlNKGc.png",r="/assets/Admin_administer.CIYc6Wya.png",l="/assets/Admin_creategroup.C8taY2VW.png",p="/assets/Admin_creategroup2.ByhMyUWk.png",s="/assets/Admin_createuser.fgu7YXJh.png",u="/assets/Admin_createuser_csv.AJp7XJuD.png",c="/assets/Admin_createuser_csv_options.DJ6LoBW3.png",d="/assets/Admin_createuser_csv_error_resume.c9eiauRP.png",m="/assets/Admin_createuser_csv_community.DdevzPet.png",g="/assets/Admin_createuser2.DqeEDpAg.png",A=JSON.parse('{"title":"Creating users and groups","description":"","frontmatter":{},"headers":[],"relativePath":"doc/guides/installation/user-and-group-generation.md","filePath":"doc/guides/installation/user-and-group-generation.md"}'),h={name:"doc/guides/installation/user-and-group-generation.md"};function w(f,e,b,v,y,_){return n(),a("div",null,e[0]||(e[0]=[i('<h1 id="creating-users-and-groups" tabindex="-1">Creating users and groups <a class="header-anchor" href="#creating-users-and-groups" aria-label="Permalink to &quot;Creating users and groups&quot;">​</a></h1><p>The Browse module content is publicly accessible by anonymous users, the Create module, however, requires authentication to access.</p><p>That&#39;s the reason why we must define users and user groups prior start working with the software.</p><h2 id="administrator-login" tabindex="-1">Administrator login <a class="header-anchor" href="#administrator-login" aria-label="Permalink to &quot;Administrator login&quot;">​</a></h2><p>The next steps will be performed on the Browse module and from an administrator account, the same that we would have defined during installation.</p><p>First, we will login by clicking on upper right menu bar <em>Sign on to</em> -&gt; <em>Browse module</em> button.</p><p><img src="'+o+'" alt="Admin login"></p><p>You will be redirected to CAS (Central Authentication Service) page. On this page we will type <strong>admin.email</strong> and <strong>admin.password</strong> on its login form.</p><p>If we have any problems with our password, we can click on the <em>Forgot your password?</em> link to reset it. After that, the Browse module navigation bar will change and a link to the Create module will appear on the right upper corner.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You cannot access the Create module directly from an admin account, you need to create a user account to get access through it</p></div><p>On the options on upper right menu bar we will click on <strong>Administer</strong>.</p><p><img src="'+r+'" alt="Admin administer"></p><p>The menu bar will change to administrator mode and more options will be accessible.<br> When clicking on <em>Access control</em>, three options will be displayed, we will use these two:</p><ul><li>Groups: To build groups of users.</li><li>E-People: To create, modify and delete ioChem-BD users</li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Prior creating users, it is advised to <a href="#creating-groups">create the user groups</a> and the <a href="./publishing-endpoints-definition.html">publication endpoints</a>, those information will be used during user creation.</p></div><p>Otherwise you can use <a href="#batch-generation-using-csv-format">Batch generation using .csv format</a> procedure to create all of them in one single operation.</p><h2 id="creating-groups" tabindex="-1">Creating groups <a class="header-anchor" href="#creating-groups" aria-label="Permalink to &quot;Creating groups&quot;">​</a></h2><p>User groups are logical associations of users, we can gather them by research group, team leader, or even create a default group with all users inside it.</p><p>Users can belong to multiple groups, but in this case they must also have a default main group defined.</p><p>To create a group, click on <em>Access control</em> and then on <em>Groups</em> menu button. On the next form we will click on <em>Create group</em>.</p><p><img src="'+l+'" alt="Admin create group form"></p><p>On the next form we will:</p><ul><li>Set group name and</li><li>assign users to group (we can do this at any time later)</li></ul><p>then click on <strong>Update group</strong> button, the group will be created.</p><p><img src="'+p+'" alt="Admin assign group users form"></p><h2 id="creating-users" tabindex="-1">Creating users <a class="header-anchor" href="#creating-users" aria-label="Permalink to &quot;Creating users&quot;">​</a></h2><p>Administrator has two mechanisms to generate user accounts:</p><ul><li><a href="#batch-generation-using-csv-format">Batch generation</a> using a csv formatted file</li><li><a href="#manual-generation-using-web-forms">Manual generation</a> using web forms</li></ul><p>It is advised to use batch generation mechanism initially to speed up account generation. After that, adding new users can be done with either manual or batch mechanism.</p><h3 id="batch-generation-using-csv-format" tabindex="-1">Batch generation using .csv format <a class="header-anchor" href="#batch-generation-using-csv-format" aria-label="Permalink to &quot;Batch generation using .csv format&quot;">​</a></h3><p>We can use this webpage to speed up user and group generation.</p><p>It will take as input a group of csv formatted lines and process it, performing automatically:</p><ul><li>User creation (with automatic username generation)</li><li>User group creation (if the provided group name doesn&#39;t exist)</li><li><a href="./publishing-endpoints-definition.html">Publication endpoint</a> generation (if option is checked)</li><li>Send password reset email to all new users (if option is checked)</li></ul><p>We will focus on the <em>Access control</em> menu. When clicking on it, three options will be displayed, we will pick on first one, <em>E-People</em>. From Administer EPerson page we will click on the <em>Add multiple E-Person (csv)...</em> button:</p><p><img src="'+s+'" alt="Admin create user form"></p><p>Next page will display a large textarea where we will paste the list of users to generate.</p><p>Pasted text must follow this rules:</p><ul><li>All fields must have a value</li><li>All fields must be delimited by semicolons ;</li><li>Don&#39;t need to use double quotes on text fields, even if they contain white spaces</li><li>You can&#39;t use diacritics and symbols on user first and last name and also on user&#39;s group name.</li></ul><p><img src="'+u+`" alt="Admin create user via batch process"></p><p>Every line will contain all information about a single user. It will appear in this order:</p><ol><li>User email</li><li>User first name</li><li>User last name</li><li>User group (the group of users where he/she will belong to)</li></ol><p>The user group field is used to easily build user groups, so users with same user group field will be included in that group. Example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>malvarez@iochem-bd.es;Moisés;Álvarez Moreno;Carles Bo group</span></span>
<span class="line"><span>jprats@iochem-bd.es;Joan;Prats Rubio;Carles Bo group</span></span>
<span class="line"><span>mperez@iochem-bd.es;Miguel;Pérez Álvarez;Carles Bo group</span></span>
<span class="line"><span>jbel@iochem-bd.es;Jose;Bel Ortiz;Nuria López group</span></span>
<span class="line"><span>ncolet@iochem-bd.es;Núria;Colet Serrano;Nuria López group</span></span></code></pre></div><p>So if we use this lines, ioChem-BD will generate five users and two groups of users:</p><ul><li>Carles Bo group : with users Moises, Joan and Miguel</li><li>Núria Lopez group : with users Jose and Núria</li></ul><p>In this case we have named our user groups with the name of the group leader but you can use other naming convention as can be the group research type or whatever that suit your needs.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>If the user groups already exists and you only want to append the new users to it, please set the names of the groups <strong>equals to the existing ones</strong>. Different group names will generate new user groups and a new publishing communities.</p></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You can check group names on top of the same page.</p></div><h4 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h4><p>There are two checkbox options at the bottom of this page that will help on other side aspects of user generation process.</p><p><img src="`+c+'" alt="Admin create user options"></p><p>First one is labeled <em>Generate publishing communities for each user group and assign created users to it</em>. It will help us on defining publishing endpoints on Browse module.</p><p>If it is checked, the process will also generate a community with the name of the user group where our new users can publish their content into Browse module.</p><p>Last option is labeled <em>Send reset password email to generated users</em>. If checked, it will send a <em>reset password</em> email to all newly created users, so they can set their account password to start using ioChem-BD software.</p><p>Finally, a textarea will appear with a resume of all unprocessed lines, displaying the reason why they failed. So its administrator duty to correct such lines and create those users again (only the wrong ones).</p><p><img src="'+d+'" alt="Admin create users via csv, error resume"></p><p>Using previous example lines would generate two publishing communities called <em>Carles Bo group</em> and <em>Núria Lopez group</em>.</p><p><img src="'+m+'" alt="Admin automatically generated publishing communities"></p><p>In case you need a more complex publishing structure please uncheck this option and read carefully <a href="./publishing-endpoints-definition.html">Publishing endpoints definition</a> page.</p><h3 id="manual-generation-using-web-forms" tabindex="-1">Manual generation using web forms <a class="header-anchor" href="#manual-generation-using-web-forms" aria-label="Permalink to &quot;Manual generation using web forms&quot;">​</a></h3><p>From Administer EPerson page we will click on the <em>Add E-Person...</em> button: <img src="'+s+'" alt="Admin create user form"> On the next form we will :</p><ul><li>check <strong>can log in</strong></li><li>set the user&#39;s e-mail on <strong>E-mail</strong> textbox</li><li>add additional user information</li><li>Assign the user groups the current user belongs to</li></ul><p>This is an example of a user account creation:</p><p><img src="'+g+'" alt="Admin create user"></p><p>Finally we click on the <strong>Reset password</strong>&#39; button, the user will then be saved in the database and a reset password link will be sent to the user&#39;s e-mail.</p>',65)]))}const x=t(h,[["render",w]]);export{A as __pageData,x as default};
